/**
  *@filename:J
  *@author: pursuit
  *@created: 2021-09-28 16:23
**/
#include <bits/stdc++.h>
#define debug(a) cout << "debug : " << (#a)<< " = " << a << endl

using namespace std;

typedef pair<int,int> pii;
typedef long long ll;
const int N = 1e5 + 10;
const int P = 1e9 + 7;
const int INF = 0x3f3f3f3f;

//一颗具有n个结点的树，顶点编号从1~n。一开始A在s点，B在t点。
//他们轮流在树上移动，A先走动。每个玩家都必须选择与其所在位置相邻的点，同时到达新点之后将一处旧顶点和与其相连的所有边。每个玩家尽可能的移动。分数为移动次数。
//每个人都想最大化自己的分数减其他人的分数。问游戏结束ScoreA - ScoreB为多少。
void solve(){
}
int main(){	
    solve();
    return 0;
}